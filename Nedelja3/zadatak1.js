//Написати функцију која враћа максимум од 3 броја (Параметри функције су 3 броја)

function maxOf3(broj1, broj2, broj3) {
  return Math.max(broj1, broj2, broj3);
}
app.innerHTML += `${maxOf3(4, 1, 7)}`;
